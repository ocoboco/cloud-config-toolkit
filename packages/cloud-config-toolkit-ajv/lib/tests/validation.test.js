const Ajv = require('ajv');

const Validation = require('../validation');

describe('Validation', function() {
  test('validates configuration according to schema', function() {
    expect(true).toBeTruthy();
  });
});